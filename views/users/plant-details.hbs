{{!-- <h2>Name: {{plantDetails.name}}</h2>

<img src="{{plantDetails.imageUrl}}">

<p>Care: {{plantDetails.plantCare}}</p>
<p>Placement: {{plantDetails.placement}}</p>
<p>Observations: {{plantDetails.observations}}</p>
<br>
<a href="/plants">Back</a> --}}

{{!-- {{#if userInSession}} --}}
<div class="container">
  <h1>{{plantDetails.name}}</h1>
        <div class="col-md-4">
  <div class="card" align="center">
    <img class="card-img-top" src="{{plantDetails.imageUrl}}" alt="{{plantDetails.name}}"/>
    <div class="card-body">
      <h5 class="card-title">{{plantDetails.name}}</h5>
      <p class="card-text">Plant Care: {{plantDetails.plantCare}}</p>
      <p class="card-text">Placement: {{plantDetails.placement}}</p>
      <p class="card-text">Observations: {{plantDetails.observations}}</p>
      <form action="/plants/{{_id}}/delete" method="POST">
        <button type="submit" class="submit" align="center">Delete this plant</button>
      </form>
    </div>
  </div>
</div>
</div>

<a href="/plants">Back</a>

{{!-- {{else}} --}}
    <p>You need to be logged in to acces the user profile! </p>

      <div class="main">
    <form action="/login" method="POST">
    <p class="sign" align="center">Login</p>
    <form class="form1">
      <input type="email" id="input-email" name="email" class="un " align="center" placeholder="Email" required >
      <input type="password" name="password" id="input-password" input class="pass" align="center" placeholder="Password" required>
      <button type="submit" class="submit" align="center">Sign in</button>
      {{!-- <p class="forgot" align="center"><a href="#">Forgot Password?</p> --}}
{{!-- {{/if}} --}}